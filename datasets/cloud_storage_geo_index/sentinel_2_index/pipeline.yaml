# Copyright 2021 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

---
resources:

  - type: bigquery_table
    # Required Properties:
    table_id: sentinel_2_index

    # Description of the table
    description: "sentinel_2_index dataset"
dag:
  initialize:
    dag_id: sentinel_2_index
    default_args:
      owner: "Google"

      # When set to True, keeps a task from getting triggered if the previous schedule for the task hasn’t succeeded
      depends_on_past: False
      start_date: '2021-03-01'
    max_active_runs: 1
    schedule_interval: "@daily"  # run once a week at Sunday 12am
    catchup: False
    default_view: graph

  tasks:

    - operator: "KubernetesPodOperator"

      # Task description
      description: "Run CSV transform within kubernetes pod"

      args:

        task_id: "cloud_storage_geo_index_sentinel_2_transform_csv"
        
        startup_timeout_seconds: 600

        # The name of the pod in which the task will run. This will be used (plus a random suffix) to generate a pod id
        name: "cloud_storage_geo_index" 

        # The namespace to run within Kubernetes. Always set its value to "default" because we follow the guideline that KubernetesPodOperator will only be used for very light workloads, i.e. use the Cloud Composer environment's resources without starving other pipelines.
        namespace: "default"
        
        image_pull_policy: "Always"

        # Docker images will be built and pushed to GCR by default whenever the `scripts/generate_dag.py` is run. To skip building and pushing images, use the optional `--skip-builds` flag.
        image: "{{ var.json.cloud_storage_geo_index.container_registry.run_csv_transform_kub_sentinel_2_index }}"

        # Set the environment variables you need initialized in the container. Use these as input variables for the script your container is expected to perform.
        env_vars:
          SOURCE_URL: "https://storage.googleapis.com/gcp-public-data-sentinel-2/index.csv.gz" 
          SOURCE_FILE: "files/data.csv"
          TARGET_FILE: "files/data_output.csv"
          TARGET_GCS_BUCKET: "{{ var.json.shared.composer_bucket }}"
          TARGET_GCS_PATH: "data/cloud_storage_geo_index/sentinel_2_index/data_output.csv"

        # Set resource limits for the pod here. For resource units in Kubernetes, see https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#resource-units-in-kubernetes
        resources:
          limit_memory: "4G"
          limit_cpu: "1"

    - operator: "GoogleCloudStorageToBigQueryOperator"
      description: "Task to load CSV data to a BigQuery table"

      args:
        task_id: "load_cloud_storage_geo_index_sentinel_2_to_bq"

      # The GCS bucket where the CSV file is located in.
        bucket: "{{ var.json.shared.composer_bucket }}"

      # The GCS object path for the CSV file
        source_objects: ["data/cloud_storage_geo_index/sentinel_2_index/data_output.csv"]
        source_format: "CSV"
        destination_project_dataset_table: "cloud_storage_geo_index.sentinel_2_index"

      # Use this if your CSV file contains a header row
        skip_leading_rows: 1

      # How to write data to the table: overwrite, append, or write if empty
      # See https://cloud.google.com/bigquery/docs/reference/auditlogs/rest/Shared.Types/WriteDisposition
        write_disposition: "WRITE_TRUNCATE"

      # The BigQuery table schema based on the CSV file. For more info, see
      # https://cloud.google.com/bigquery/docs/schemas.
      # Always use snake_case and lowercase for column names, and be explicit,
      # i.e. specify modes for all columns.

        schema_fields:
          - name: "granule_id"
            type: "STRING"
            mode: "required"
          - name: "product_id"
            type: "STRING"
            mode: "NULLABLE"
          - name: "datatake_identifier"
            type: "STRING"
            mode: "NULLABLE"
          - name: "mgrs_tile"
            type: "STRING"
            mode: "NULLABLE"
          - name: "sensing_time"
            type: "TIMESTAMP"
            mode: "NULLABLE"
          - name: "geometric_quality_flag"
            type: "STRING"
            mode: "NULLABLE"
          - name: "generation_time"
            type: "TIMESTAMP"
            mode: "NULLABLE"
          - name: "north_lat"
            type: "FLOAT"
            mode: "NULLABLE"
          - name: "south_lat"
            type: "FLOAT"
            mode: "NULLABLE"
          - name: "west_lon"
            type: "FLOAT"
            mode: "NULLABLE"
          - name: "east_lon"
            type: "FLOAt"
            mode: "NULLABLE"
          - name: "base_url"
            type: "STRING"
            mode: "NULLABLE"
          - name: "total_size"
            type: "INTEGER"
            mode: "NULLABLE"
          - name: "cloud_cover"
            type: "FLOAT"
            mode: "NULLABLE"
        
  graph_paths: 
    - "cloud_storage_geo_index_sentinel_2_transform_csv >> load_cloud_storage_geo_index_sentinel_2_to_bq"
